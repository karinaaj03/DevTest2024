import React from 'react';
import styles from '../../Styles/commons/Header.module.css'

const Header = () => {
    return (
        <div className={styles.container}>
            <p>Online Polls</p>
        </div>
    );
};

export default Header;